set guifont=Source\ Code\ Pro\ for\ Powerline:h14 "make sure to escape the spaces in the name properly
set background=dark
set clipboard=unnamed,autoselect " share clipboard. Make sure with 'vim --version | grep clipboard' if your vim is compatible with clipboard
set autowrite " autosave when you swap between files. Without this setting, Errors are out!
